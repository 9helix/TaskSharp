<Window x:Class="TaskSharp.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskSharp"
        mc:Ignorable="d"
        Title="TaskSharp - Nadzorna ploča" Height="520" Width="800" ResizeMode="CanMinimize" Loaded="Dashboard_Loaded">
    <Grid Background="DeepSkyBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="113"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock FontSize="40" HorizontalAlignment="Center" Width="109" TextAlignment="Center"><Run Text="Task"/><Run Text="#"/></TextBlock>

        <ListBox Grid.Column="0" Background="DeepSkyBlue" Foreground="White" x:Name="sidebar" Margin="0,67,0,0" Padding="5,0,0,0" BorderThickness="0" FontWeight="Bold" SelectionChanged="sidebar_SelectionChanged" FontSize="14">
            <ListBoxItem Height="37" Margin="0,10,0,0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/Images/Notes/note.png" Width="13"/>
                    <TextBlock Margin="5,0,0,0"><Run Text="Bilješke"/></TextBlock>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem Height="37">
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/Images/Notes/event.png" Width="13"/>
                    <TextBlock Margin="5,0,0,0"><Run Text="Događaji"/></TextBlock>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem Height="37">
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/Images/Notes/reminder.png" Width="13"/>
                    <TextBlock Margin="5,0,0,0"><Run Text="Podsjetnici"/></TextBlock>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem Height="37">
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/Images/Notes/todo.png" Width="13"/>
                    <TextBlock Margin="5,0,0,0"><Run Text="To-do liste"/></TextBlock>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem Height="35" Margin="0,204,0,0" >
                <StackPanel Orientation="Horizontal">
                    <Image Source="Resources/Images/logout.png" Width="13"/>
                    <TextBlock Margin="5,0,0,0"><Run Text="Odjava"/></TextBlock>
                </StackPanel>
            </ListBoxItem>
        </ListBox>

        <Frame Grid.Column="1" x:Name="navframe" NavigationUIVisibility="Hidden" Background="white" />

        <Button Click="Create_Click" Background="DeepSkyBlue" Foreground="White" FontSize="14" x:Name="CreateButton" Margin="0,435,34,0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Width="110" Padding="1,1,1,3" Grid.IsSharedSizeScope="True" Height="25">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
            <StackPanel Orientation="Horizontal">
                <Image Source="/Resources/Images/add.png" Width="13"/>
                <TextBlock Margin="5,0,0,0"><Run Text="Dodaj novo"/></TextBlock>
            </StackPanel>
        </Button>
    </Grid>
</Window>
